@model Marketing.GestaoVerbas.Core.Grid.GridSetings<Marketing.GestaoVerbas.ViewModel.FilialEmpresaViewModel>

@{
    AjaxOptions ajaxOptions = new AjaxOptions()
    {
        UpdateTargetId = "appendFindFilialEmpresa",
        InsertionMode = InsertionMode.Replace,
        OnBegin = "DefaultAjaxBegin",
        OnComplete = "DefaultAjaxComplete",
        OnSuccess = "findFilialEmpresaResult",
        OnFailure = "DefaultAjaxFailure",
        HttpMethod = "POST"
    };
}



@using (Ajax.BeginForm("Pesquisar", "FilialEmpresa", ajaxOptions, new { @id = "frmPesquisaFilialEmpresa", @class = "form-pagination" }))
{
    @Html.AntiForgeryToken()

    <div class="modal" tabindex="-1" role="dialog" id="mdlPesquisaFilialEmpresa">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header bg-primary" style="color:white;">
                    <h5 class="modal-title" id="myModalLabel">Selecionar Filial Empresa</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <div class="modal-body bg-light">
                    <div class="container card bg-primary divFiltrosPesquisa">
                        <div class="py-1" style="color:white;">
                            <h5>Pesquisar</h5>
                        </div>
                        <div class="row bg-light">
                            <div class="col-4">
                                @Html.LabelFor(model => model.Filtro.CodFilialEmpresa)
                                @Html.EditorFor(model => model.Filtro.CodFilialEmpresa, new { htmlAttributes = new { @class = "form-control", data_val = false } })
                            </div>
                            <div class="col-6">
                                @Html.LabelFor(model => model.Filtro.NomFilialEmpresa)
                                @Html.EditorFor(model => model.Filtro.NomFilialEmpresa, new { htmlAttributes = new { @class = "form-control", data_val = false } })
                            </div>
                            <div class="col-2">
                                <label>&nbsp;</label>
                                <br />
                                <button type="submit" id="btnPesquisarFilialEmpresa" class="btn btn-sm btn-primary" title="Pesquisar" value="Pesquisar">Pesquisar</button>
                            </div>
                        </div>
                    </div>
                    <br />
                    <div class="container card bg-primary">
                        <div class="row bg-secondary">
                            <div class="table-responsive">
                                <div class="dataTables_wrapper">
                                    <table class="table table-striped table-bordered table-hover dataTable">
                                        <thead class="bg-secondary">
                                            <tr>
                                                <th>Ações</th>
                                                <th>
                                                    <button type="submit" class="" style="border:none;" sort-expression="NomFilialEmpresa">
                                                        @Html.DisplayNameFor(model => model.Filtro.CodFilialEmpresa)
                                                    </button>
                                                </th>
                                                <th>
                                                    <button type="submit" class="" style="border:none;" sort-expression="NomFilialEmpresa">
                                                        @Html.DisplayNameFor(model => model.Filtro.NomFilialEmpresa)
                                                    </button>
                                                </th>
                                                <th>
                                                    <button type="submit" class="" style="border:none;" sort-expression="CodEmpresa">
                                                        @Html.DisplayNameFor(model => model.Filtro.CodEmpresa)
                                                    </button>
                                                </th>
                                                <th>
                                                    <button type="submit" class="" style="border:none;" sort-expression="NomEmpresa">
                                                        @Html.DisplayNameFor(model => model.Filtro.NomEmpresa)
                                                    </button>
                                                </th>
                                            </tr>
                                        </thead>
                                        <tbody class="bg-light border">
                                            @foreach (var item in Model.Result)
                                            {
                                                <tr>
                                                    <td>
                                                        <div class="btn-group">
                                                            <button type="button" class="btn btn-sm btn-primary fa fa-check" onclick="findFilialEmpresaSelect(@item.CodFilialEmpresa, '@item.NomFilialEmpresa')"></button>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.CodFilialEmpresa)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.NomFilialEmpresa)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.CodEmpresa)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.NomEmpresa)
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            @Html.Partial(Marketing.GestaoVerbas.Core.Grid.Constantes.VIEW_PAGINACAO, Model.Setings, new ViewDataDictionary { TemplateInfo = new TemplateInfo { HtmlFieldPrefix = "Setings" } })


                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" data-dismiss="modal" aria-label="Close" class="btn btn-sm btn-warning">Fechar</button>
                </div>
            </div>
        </div>
    </div>
}

